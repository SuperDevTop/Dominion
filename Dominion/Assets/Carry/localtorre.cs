using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class localtorre : MonoBehaviour
{
    public float localx;
    public float localy;
    public float localz;
    public GameObject torre;
    // Start is called before the first frame update
    void Start()
    {
      //  torre.gameObject.transform.localPosition = new Vector3(localx, localy, localz);
    }

    // Update is called once per frame
    void Update()
    {

        //torre.gameObject.transform.localPosition = new Vector3(localx, localy, localz);
    }
}
